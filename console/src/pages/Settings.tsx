// Settings page — stub, will be replaced
export default function SettingsPage() {
  return (
    <div
      style={{
        padding: "2rem",
        color: "var(--text-muted)",
        fontFamily: "var(--font)",
        fontSize: "0.8rem",
      }}
    >
      Settings — coming soon
    </div>
  );
}
