// Timeline page — stub, will be replaced
export default function TimelinePage() {
  return (
    <div
      style={{
        padding: "2rem",
        color: "var(--text-muted)",
        fontFamily: "var(--font)",
        fontSize: "0.8rem",
      }}
    >
      Timeline — coming soon
    </div>
  );
}
