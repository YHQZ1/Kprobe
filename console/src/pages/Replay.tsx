// Replay page — stub, will be replaced
export default function ReplayPage() {
  return (
    <div
      style={{
        padding: "2rem",
        color: "var(--text-muted)",
        fontFamily: "var(--font)",
        fontSize: "0.8rem",
      }}
    >
      Replay — coming soon
    </div>
  );
}
